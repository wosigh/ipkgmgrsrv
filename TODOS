* Add better comments (everywhere).

* Standardize return values, some functions use unix style 
  (returnValue:0 equals no errors). These should be switched to palm style 
  (returnValue: true equals no errors).
  
* Add confirmation support.
