* Get i686 build working.

* Add better comments (everywhere).

* Fix makefiles so that they build objects.

* Make sure all json responses use keys over arays when possible.

* Make sure subscriptions only reply to the app that requested the info.

* Format the subscription/callback responses into json.
  
* Add confirmation support.

* Check for memory leaks.
